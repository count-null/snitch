import { sleep, log } from "./helpers.js";

export async function createSnapshot(lnd, CONFIG) {
  log("Creating Snapshot");
}

export async function cleanSnapshots(lnd, CONFIG) {
  log("Cleaning Snapshots");
}
